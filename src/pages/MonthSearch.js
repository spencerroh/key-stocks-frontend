import React from 'react';
import Card from '../components/Card';

const MonthSearch = props => {
    return (
        <Card className="text-lg col-start-2" title="월별 검색">
            작업 중
        </Card>
    );
};

export default MonthSearch;